---
# ========================================================
# SKU: Slinky-App-Standalone-v3.2-YamlFix
# 修正：
#   1. 修正 static-nfs.yaml 縮排問題 (移除前導空白)
#   2. 忽略 Namespace 已存在的錯誤訊息
# ========================================================
- name: Application Layer - Deploy Slurm (Static NFS)
  hosts: maasjuju
  gather_facts: yes
  vars:
    test_model: "slinky-cluster"
    release_name: "slinky"
    namespace: "slurm-cluster"
    slurm_git_url: "https://github.com/stackhpc/slurm-k8s-cluster.git"
    nfs_path: "/srv/nfs/kubedata"

  tasks:
    # -----------------------------------------------------------
    # 1. 準備工作 & 獲取 IP
    # -----------------------------------------------------------
    - name: 1.1 確認 Machine 0 存活
      shell: juju ssh --model {{ test_model }} 0 -- "echo 'alive'"
      register: machine_check
      ignore_errors: yes

    - name: 1.2 獲取 Machine 0 內網 IP (NFS Server IP)
      shell: |
        juju status --model {{ test_model }} --format json | jq -r '.machines["0"]["dns-name"]'
      register: nfs_server_ip

    # -----------------------------------------------------------
    # 2. 確保 NFS Server 運作中 (Host 端)
    # -----------------------------------------------------------
    - name: 2.1 安裝並設定 NFS Server
      shell: |
        juju ssh --model {{ test_model }} 0 -- \
        "sudo apt-get update && \
         sudo DEBIAN_FRONTEND=noninteractive apt-get install -y nfs-kernel-server && \
         sudo mkdir -p {{ nfs_path }} && \
         sudo chown nobody:nogroup {{ nfs_path }} && \
         sudo chmod 777 {{ nfs_path }} && \
         echo '{{ nfs_path }} *(rw,sync,no_subtree_check,no_root_squash)' | sudo tee /etc/exports && \
         sudo exportfs -a && \
         sudo systemctl restart nfs-kernel-server"
      changed_when: true

    # -----------------------------------------------------------
    # 3. [關鍵修正] 手動建立 Static PV 和 PVC
    # -----------------------------------------------------------
    - name: 3.1 建立 PV/PVC 定義檔 (靠左對齊修正)
      # 使用 <<-EOF 可以忽略前導 Tab，但為了保險，我們直接靠左寫
      shell: |
        juju ssh --model {{ test_model }} 0 -- "cat <<EOF > /home/ubuntu/static-nfs.yaml
        apiVersion: v1
        kind: PersistentVolume
        metadata:
          name: slurm-nfs-pv
        spec:
          capacity:
            storage: 20Gi
          accessModes:
            - ReadWriteMany
          persistentVolumeReclaimPolicy: Retain
          nfs:
            path: {{ nfs_path }}
            server: {{ nfs_server_ip.stdout }}
        ---
        apiVersion: v1
        kind: PersistentVolumeClaim
        metadata:
          name: slurm-shared-storage
          namespace: {{ namespace }}
        spec:
          accessModes:
            - ReadWriteMany
          resources:
            requests:
              storage: 20Gi
          volumeName: slurm-nfs-pv
          storageClassName: \"\"
        EOF"
      changed_when: false

    - name: 3.2 應用 PV/PVC
      # 忽略 Namespace 已經存在的錯誤
      shell: |
        juju ssh --model {{ test_model }} 0 -- \
        "kubectl create ns {{ namespace }} 2>/dev/null || true && \
         kubectl apply -f /home/ubuntu/static-nfs.yaml"
      changed_when: true

    # -----------------------------------------------------------
    # 4. 部署 Slurm (使用現成 PVC)
    # -----------------------------------------------------------
    - name: 4.1 下載 StackHPC Chart
      shell: |
        juju ssh --model {{ test_model }} 0 -- \
        "sudo apt-get install -y git && \
         rm -rf slurm-k8s-cluster && \
         git clone --depth 1 {{ slurm_git_url }}"
      changed_when: true

    - name: 4.2 下載依賴
      shell: |
        juju ssh --model {{ test_model }} 0 -- \
        "cd slurm-k8s-cluster/slurm-cluster-chart && helm dependency update"
      changed_when: true

    - name: 4.3 產生 Values (Disable Creation)
      # 告訴 Helm 不要自己建 PVC (create: false)
      shell: |
        juju ssh --model {{ test_model }} 0 -- "cat <<EOF > /home/ubuntu/slurm-static-values.yaml
        global:
          clusterName: {{ test_model }}

        # [儲存設定 - 靜態綁定模式]
        volumeClaim:
          create: false
          name: slurm-shared-storage

        controller:
          size: 10Gi
          storageClassName: \"local-path\"
          resources:
            limits:
              cpu: 2
              memory: 4Gi

        slurmdbd:
          enabled: true
          storageClassName: \"local-path\"
          size: 10Gi

        login:
          service:
            type: NodePort
            nodePort: 30022
          ssh:
            enabled: true

        worker:
          replicas: 1
          resources:
            limits:
              cpu: 8
              memory: 16Gi
        EOF"
      changed_when: false

    - name: 4.4 部署 Slurm
      # 加上 --wait，耐心等待所有 Pod 跑起來
      shell: |
        juju ssh --model {{ test_model }} 0 -- \
        "helm upgrade --install {{ release_name }} ./slurm-k8s-cluster/slurm-cluster-chart \
          --namespace {{ namespace }} \
          --create-namespace \
          -f /home/ubuntu/slurm-static-values.yaml \
          --wait --timeout 15m"
      register: helm_deploy

    - name: ✅ 完成
      debug:
        msg: 
          - "🎉 Slurm + Static NFS (V3.2) 部署成功！"
          - "PVC 狀態檢查："
          - "kubectl get pvc -n slurm-cluster"
