---
# ========================================================
# Scenario: Slinky Offline AI Environment - Deployment Only
# Goal: Upload files & Prep Env, BUT DO NOT RUN JOBS.
# ========================================================
- name: 佈署 Slinky PyTorch 測試檔案 (不自動執行)
  hosts: maasjuju
  gather_facts: no
  vars:
    # ---- 本地檔案位置 (Jumpbox) ----
    local_wheel_path: "/home/gary/torch.whl"
    
    # ---- Slinky 設定 ----
    test_model: "slinky-cluster"
    k8s_machine: "0"
    slurm_ns: "slurm"
    controller_pod: "slurm-controller-0"
    shared_dir: "/home/slurm"

  tasks:
    # ----------------------------------------------------
    # 1. 檢查與確認
    # ----------------------------------------------------
    - name: 1.1 檢查 Jumpbox 上是否有 torch.whl
      stat:
        path: "{{ local_wheel_path }}"
      register: wheel_stat
      failed_when: not wheel_stat.stat.exists

    - name: 1.2 確認 Slurm Controller 活著
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- \
          "kubectl get pod -n {{ slurm_ns }} {{ controller_pod }} -o jsonpath='{.status.phase}'"
      register: pod_status
      failed_when: "'Running' not in pod_status.stdout"

    # ----------------------------------------------------
    # 2. 搬運大檔案 (Wheel)
    # ----------------------------------------------------
    - name: 2.1 上傳 torch.whl 到 Controller 共享目錄
      shell: |
        # 1. Jumpbox -> Machine 0
        juju scp {{ local_wheel_path }} {{ test_model }}/{{ k8s_machine }}:/tmp/torch.whl
        
        # 2. Machine 0 -> Pod (NFS Shared Dir)
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- \
          "kubectl cp /tmp/torch.whl {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/torch.whl"
        
        # 3. 清除暫存
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- "rm -f /tmp/torch.whl"
      async: 1200
      poll: 10

    # ----------------------------------------------------
    # 3. 直接安裝 PyTorch (不使用 venv)
    # ----------------------------------------------------
    - name: 3.1 離線安裝 PyTorch (使用 --user 模式)
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          # 直接透過 kubectl 執行複雜指令
          kubectl exec -n {{ slurm_ns }} {{ controller_pod }} -- bash -c '
            set -e
            cd {{ shared_dir }}
            
            # 確保 Path 包含 user base (讓 pip 安裝的指令可以用)
            export PATH=$HOME/.local/bin:$PATH

            echo "🔍 Checking Python environment..."
            
            # 檢查是否已安裝
            if python3 -c "import torch" >/dev/null 2>&1; then
              echo "✅ PyTorch already installed, skipping."
            else
              echo "📦 Installing PyTorch from Offline Wheel (User Mode)..."
              
              # [關鍵修正] 使用 --user 避開權限問題，並加上 --break-system-packages (針對 Python 3.11+)
              # 注意：因為是 Container 環境，我們不需要擔心弄髒系統，加上 --break-system-packages 是安全的
              pip3 install {{ shared_dir }}/torch.whl \
                --no-index \
                --find-links {{ shared_dir }} \
                --user \
                --break-system-packages
            fi
            
            # 驗證
            echo "🔍 Verifying installation..."
            python3 -c "import torch; print(f\"🎉 Success! PyTorch {torch.__version__} is ready.\")"
          '
        EOS
      async: 900
      poll: 10
      register: install_res

    # ----------------------------------------------------
    # 4. 佈署測試腳本 (30秒倒數 + 可中斷)
    # ----------------------------------------------------
    - name: 4.1 注入 train.py (30秒自動倒數，可 Ctrl+C)
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          cat << 'PY' > /tmp/train.py
        import torch
        import time
        import os
        import socket
        import sys

        # 1. 設定
        TARGET_DURATION = 30  # 設定跑 30 秒
        node_name = socket.gethostname()
        job_id = os.environ.get('SLURM_JOB_ID', 'Interactive')

        print(f"\n🚀 [Job {job_id}] GPU Stress Test (Duration: {TARGET_DURATION}s)")
        print(f"📍 Node: {node_name}")
        print("-" * 65)

        # 2. 檢查 GPU
        if torch.cuda.is_available():
            gpu_name = torch.cuda.get_device_name(0)
            print(f"✅ GPU Detected: {gpu_name}")
            dev = torch.device("cuda")
        else:
            print("❌ No GPU found! Running on CPU.")
            dev = torch.device("cpu")

        # 3. 準備矩陣
        N = 10000
        print(f"🔥 Allocating Tensors ({N}x{N})...")
        a = torch.randn(N, N, device=dev)
        b = torch.randn(N, N, device=dev)

        print(f"⏳ Starting Countdown... Press [Ctrl+C] to stop early.")
        print("-" * 65)

        # 4. 執行迴圈
        start_time = time.time()
        count = 0

        try:
            while True:
                # 計算經過時間
                current_time = time.time()
                elapsed = current_time - start_time
                remaining = TARGET_DURATION - elapsed

                # 時間到就自動跳出
                if remaining <= 0:
                    break

                # 運算負載
                c = torch.mm(a, b)
                torch.cuda.synchronize() # 等待 GPU 算完
                
                count += 1
                
                # 動態顯示進度條
                # \r 回到行首，覆蓋舊文字
                sys.stdout.write(f"\r⚡ Batch: {count} | ⏱️ Used: {elapsed:4.1f}s | ⏳ Left: {remaining:4.1f}s ")
                sys.stdout.flush()

        except KeyboardInterrupt:
            print("\n\n🛑 Test Stopped by User (Ctrl+C).")
        
        # 5. 結算
        final_time = time.time() - start_time
        print(f"\n\n" + "="* 65)
        print(f"✅ Test Finished in {final_time:.2f} seconds.")
        print(f"📊 Total Batches Processed: {count}")
        print("="* 65 + "\n")
        PY
          
          kubectl cp /tmp/train.py {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/train.py
        EOS

    - name: 4.2 注入 run_job.sh (Slurm 排程腳本)
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          cat << 'SBATCH' > /tmp/run_job.sh
        #!/bin/bash
        #SBATCH --job-name=gpu-test
        #SBATCH --output=gpu-%j.out
        #SBATCH --ntasks=1
        #SBATCH --gres=gpu:1
        #SBATCH --time=00:05:00
        #SBATCH --partition=slinky

        echo "➡️ Loading AI Environment..."
        source {{ shared_dir }}/ai-env/bin/activate

        echo "➡️ Starting Training..."
        python {{ shared_dir }}/train.py
        SBATCH
        
          kubectl cp /tmp/run_job.sh {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/run_job.sh
        EOS

    # ----------------------------------------------------
    # 5. 手動操作指引
    # ----------------------------------------------------
    - name: 🚀 佈署完成！請手動開始測試
      debug:
        msg: 
          - "========================================================"
          - "✅ 檔案與環境已就緒，請依照下列步驟手動測試："
          - "========================================================"
          - "1. [登入 Controller Pod]"
          - "   juju ssh -m {{ test_model }} 0 -- kubectl exec -it -n slurm slurm-controller-0 -- bash"
          - ""
          - "2. [進入共享目錄]"
          - "   cd /home/slurm"
          - ""
          - "3. [方式 A: 互動式測試 (推薦! 可以直接看螢幕輸出)]"
          - "   source ai-env/bin/activate"
          - "   srun --gres=gpu:1 --pty python train.py"
          - ""
          - "4. [方式 B: 排程測試 (標準做法)]"
          - "   sbatch run_job.sh"
          - "   squeue (查看狀態)"
          - "   cat gpu-*.out (查看結果)"
          - "========================================================"
