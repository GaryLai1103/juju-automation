---
# ========================================================
# Scenario: Slinky Offline AI Environment - Deployment & Verify
# Goal: Install PyTorch (WHL) & Deploy GPU Verify Scripts
# ========================================================
- name: 佈署 Slinky PyTorch 與 GPU 驗證腳本
  hosts: maasjuju
  gather_facts: no
  vars:
    # ---- Slinky 設定 ----
    test_model: "slinky-cluster"
    k8s_machine: "0"
    slurm_ns: "slurm"
    controller_pod: "slurm-controller-0"
    shared_dir: "/home/slurm"

  tasks:
    # ----------------------------------------------------
    # 1. 離線安裝 Pip 與 Dependencies (強力清除 V3 版)
    # ----------------------------------------------------
    - name: 1.1 離線安裝所有 Wheel 套件 (Bulk Install from WHL)
      shell: |
        # =======================================================
        # [步驟 0] 使用 sh -c 強制在 Pod 內執行刪除
        # 確保舊的衝突檔案被徹底清除
        # =======================================================
        echo "🧹 Nuke ALL old wheels in Pod (Force)..."
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- \
           "kubectl exec -n {{ slurm_ns }} {{ controller_pod }} -- sh -c 'rm -f {{ shared_dir }}/*.whl'"

        # -------------------------------------------------------
        # [步驟 1] 取得 Jumpbox 上 WHL 資料夾的檔案
        # -------------------------------------------------------
        WHEEL_FILES=$(ls /home/gary/WHL/*.whl)
        
        if [ -z "$WHEEL_FILES" ]; then
            echo "❌ Error: No .whl files found in /home/gary/WHL/"
            exit 1
        fi
        
        # -------------------------------------------------------
        # [步驟 2] 迴圈：將每個檔案傳送到 Pod
        # -------------------------------------------------------
        for FILE in $WHEEL_FILES; do
            FILENAME=$(basename "$FILE")
            echo "🚀 Transferring $FILENAME..."
            
            # Jumpbox -> Machine 0
            cat "$FILE" | juju ssh -m {{ test_model }} {{ k8s_machine }} "cat > /tmp/$FILENAME"
            
            # Machine 0 -> Pod
            juju ssh -m {{ test_model }} {{ k8s_machine }} -- \
               "kubectl cp /tmp/$FILENAME {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/$FILENAME"
            
            # Clean up Machine 0
            juju ssh -m {{ test_model }} {{ k8s_machine }} -- "rm -f /tmp/$FILENAME"
        done

        # -------------------------------------------------------
        # [步驟 3] 進入 Pod 執行安裝
        # -------------------------------------------------------
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          kubectl exec -n {{ slurm_ns }} {{ controller_pod }} -- bash -c '
            set -e
            cd {{ shared_dir }}
            export PATH=$HOME/.local/bin:$PATH
            
            # 再次檢查 (確認無重複檔案)
            echo "📂 Files in directory (Should contain NO duplicates):"
            ls -1 *.whl | wc -l
            
            # --- 安裝所有 .whl 檔 ---
            echo "📦 Installing ALL wheels..."
            python3 -m pip install *.whl \
                --no-index \
                --find-links {{ shared_dir }} \
                --user \
                --break-system-packages
            
            echo "🔍 Verifying PyTorch on Controller..."
            # 簡單驗證 Controller 端安裝是否成功 (這裡 CUDA False 是正常的)
            python3 -c "import torch; print(f\"🎉 Success! PyTorch {torch.__version__} is ready.\")"
          '
        EOS
      async: 1800
      poll: 10
      register: install_res

    # ----------------------------------------------------
    # 2. 佈署 GPU 驗證腳本 (取代原本的 train.py)
    # ----------------------------------------------------
    - name: 2.1 注入 GPU 檢測程式 (gpu_test.py)
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          # 1. 建立 Python 測試檔
          cat << 'PY' > /tmp/gpu_test.py
        import torch
        import sys
        import socket
        import time

        hostname = socket.gethostname()
        print(f"========================================")
        print(f"📍 Node Name: {hostname}")
        print(f"🐍 PyTorch Version: {torch.__version__}")
        print(f"========================================")

        if torch.cuda.is_available():
            print(f"✅ SUCCESS! GPU Detected!")
            print(f"🔢 GPU Count: {torch.cuda.device_count()}")
            print(f"🏷️  GPU Name:  {torch.cuda.get_device_name(0)}")

            # 簡單矩陣運算測試
            print("🔥 Running tensor calculation on GPU (Matrix Multiplication)...")
            try:
                x = torch.randn(4096, 4096).cuda()
                y = torch.mm(x, x)
                torch.cuda.synchronize()
                print("✅ Calculation finished successfully.")
            except Exception as e:
                print(f"❌ Calculation Failed: {e}")
                sys.exit(1)
        else:
            print(f"❌ ERROR: GPU NOT detected on this node ({hostname}).")
            sys.exit(1)
        PY
          
          # 2. 傳入 Pod
          kubectl cp /tmp/gpu_test.py {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/gpu_test.py
        EOS

    - name: 2.2 注入 Slurm 排程腳本 (run_verify.sh)
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          # 1. 建立 SBATCH 腳本
          cat << 'SBATCH' > /tmp/run_verify.sh
        #!/bin/bash
        #SBATCH --job-name=gpu-verify
        #SBATCH --output=gpu-verify.out
        #SBATCH --partition=slinky
        #SBATCH --gres=gpu:1
        #SBATCH --ntasks=1
        #SBATCH --time=00:02:00

        # 設定 PATH (因為是 --user 安裝)
        export PATH=$HOME/.local/bin:$PATH

        echo "🚀 Starting GPU Verification on Worker Node..."
        python3 {{ shared_dir }}/gpu_test.py
        SBATCH
        
          # 2. 傳入 Pod
          kubectl cp /tmp/run_verify.sh {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/run_verify.sh
          
          # 3. (選用) 直接幫你送出 Job，省得你手動打指令
          echo "📤 Submitting Slurm Job automatically..."
          kubectl exec -n {{ slurm_ns }} {{ controller_pod }} -- bash -c "cd {{ shared_dir }} && sbatch run_verify.sh"
        EOS

    # ----------------------------------------------------
    # 3. 提示訊息
    # ----------------------------------------------------
    - name: 🚀 佈署完成
      debug:
        msg: 
          - "========================================================"
          - "✅ 安裝與佈署完成！GPU 測試工作已送出。"
          - "========================================================"
          - "請稍後登入檢查結果："
          - "1. 登入: juju ssh -m {{ test_model }} 0 -- kubectl exec -it -n slurm slurm-controller-0 -- bash"
          - "2. 查看: cat /home/slurm/gpu-verify.out"
          - "========================================================"
