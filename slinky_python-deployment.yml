---
# ========================================================
# Scenario: Slinky Offline AI Environment - Deployment Only
# Goal: Upload files & Prep Env (WHL Folder Version)
# ========================================================
- name: 佈署 Slinky PyTorch 測試檔案 (WHL 資料夾版)
  hosts: maasjuju
  gather_facts: no
  vars:
    # ---- Slinky 設定 ----
    test_model: "slinky-cluster"
    k8s_machine: "0"
    slurm_ns: "slurm"
    controller_pod: "slurm-controller-0"
    shared_dir: "/home/slurm"

  tasks:
    # ----------------------------------------------------
    # 1. 離線安裝 Pip 與 Dependencies (強力清除 V3 版)
    # ----------------------------------------------------
    - name: 1.1 離線安裝所有 Wheel 套件 (Bulk Install from WHL)
      shell: |
        # =======================================================
        # [修正步驟] 0. 使用 sh -c 強制在 Pod 內執行刪除
        # 這次絕對會刪乾淨！
        # =======================================================
        echo "🧹 Nuke ALL old wheels in Pod (Force)..."
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- \
           "kubectl exec -n {{ slurm_ns }} {{ controller_pod }} -- sh -c 'rm -f {{ shared_dir }}/*.whl'"

        # -------------------------------------------------------
        # 1. 取得 Jumpbox 上 WHL 資料夾的檔案
        # -------------------------------------------------------
        WHEEL_FILES=$(ls /home/gary/WHL/*.whl)
        
        if [ -z "$WHEEL_FILES" ]; then
            echo "❌ Error: No .whl files found in /home/gary/WHL/"
            exit 1
        fi
        
        # -------------------------------------------------------
        # 2. 迴圈：將每個檔案傳送到 Pod
        # -------------------------------------------------------
        for FILE in $WHEEL_FILES; do
            FILENAME=$(basename "$FILE")
            echo "🚀 Transferring $FILENAME..."
            
            # Jumpbox -> Machine 0
            cat "$FILE" | juju ssh -m {{ test_model }} {{ k8s_machine }} "cat > /tmp/$FILENAME"
            
            # Machine 0 -> Pod
            juju ssh -m {{ test_model }} {{ k8s_machine }} -- \
               "kubectl cp /tmp/$FILENAME {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/$FILENAME"
            
            # Clean up Machine 0
            juju ssh -m {{ test_model }} {{ k8s_machine }} -- "rm -f /tmp/$FILENAME"
        done

        # -------------------------------------------------------
        # 3. 進入 Pod 執行安裝
        # -------------------------------------------------------
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          kubectl exec -n {{ slurm_ns }} {{ controller_pod }} -- bash -c '
            set -e
            cd {{ shared_dir }}
            export PATH=$HOME/.local/bin:$PATH
            
            # 再次檢查 (這次應該只會看到新傳進去的那 20 幾個檔案)
            echo "📂 Files in directory (Should contain NO duplicates):"
            ls -1 *.whl | wc -l
            
            # --- 安裝所有 .whl 檔 ---
            echo "📦 Installing ALL wheels..."
            python3 -m pip install *.whl \
                --no-index \
                --find-links {{ shared_dir }} \
                --user \
                --break-system-packages
            
            echo "🔍 Verifying PyTorch..."
            # 驗證是否能抓到 CUDA
            python3 -c "import torch; print(f\"🎉 Success! PyTorch {torch.__version__} is ready. CUDA available: {torch.cuda.is_available()}\")"
            
            if python3 -c "import torch; exit(0 if torch.cuda.is_available() else 1)"; then
               echo "✅ GPU is detected!"
            else
               echo "⚠️ GPU not detected (Running on CPU mode)"
            fi
          '
        EOS
      async: 1800
      poll: 10
      register: install_res

    # ----------------------------------------------------
    # 2. 佈署測試腳本
    # ----------------------------------------------------
    - name: 2.1 注入 train.py
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          cat << 'PY' > /tmp/train.py
        import torch
        import time
        import os
        import socket
        import sys

        TARGET_DURATION = 30
        node_name = socket.gethostname()
        job_id = os.environ.get('SLURM_JOB_ID', 'Interactive')

        print(f"\n🚀 [Job {job_id}] GPU Test (Node: {node_name})")
        
        if torch.cuda.is_available():
            print(f"✅ GPU: {torch.cuda.get_device_name(0)}")
            dev = torch.device("cuda")
        else:
            print("❌ No GPU found! Using CPU.")
            dev = torch.device("cpu")

        N = 4096 # 矩陣大小
        a = torch.randn(N, N, device=dev)
        b = torch.randn(N, N, device=dev)

        print(f"🔥 Start Matrix Multiplication loop ({TARGET_DURATION}s)...")
        start_time = time.time()
        count = 0
        
        try:
            while (time.time() - start_time) < TARGET_DURATION:
                c = torch.mm(a, b)
                torch.cuda.synchronize() if torch.cuda.is_available() else None
                count += 1
                sys.stdout.write(f"\r⚡ Batch: {count}")
                sys.stdout.flush()
        except KeyboardInterrupt:
            print("\nStopped.")

        print(f"\n✅ Finished. Total Batches: {count}")
        PY
          
          kubectl cp /tmp/train.py {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/train.py
        EOS

    - name: 2.2 注入 run_job.sh
      shell: |
        juju ssh -m {{ test_model }} {{ k8s_machine }} -- bash -s <<'EOS'
          cat << 'SBATCH' > /tmp/run_job.sh
        #!/bin/bash
        #SBATCH --job-name=gpu-test
        #SBATCH --output=gpu-%j.out
        #SBATCH --ntasks=1
        #SBATCH --gres=gpu:1
        #SBATCH --time=00:05:00
        #SBATCH --partition=slinky

        # [修改點 2] 移除 source ai-env，因為我們是用 --user 安裝到預設路徑
        echo "➡️ Starting Training..."
        export PATH=$HOME/.local/bin:$PATH
        
        python3 {{ shared_dir }}/train.py
        SBATCH
        
          kubectl cp /tmp/run_job.sh {{ slurm_ns }}/{{ controller_pod }}:{{ shared_dir }}/run_job.sh
        EOS

    # ----------------------------------------------------
    # 3. 提示訊息
    # ----------------------------------------------------
    - name: 🚀 佈署完成
      debug:
        msg: 
          - "✅ 安裝完成！請登入 Controller 測試："
          - "juju ssh -m {{ test_model }} 0 -- kubectl exec -it -n slurm slurm-controller-0 -- bash"
          - "cd /home/slurm"
          - "python3 train.py"
